# Runtime dependencies for the Vercel Python serverless function (api/index.py).
# Deliberately minimal: the deployed bundle must stay well under Vercel's size
# limit, so pandas / scikit-learn / matplotlib live in requirements-dev.txt and
# are only imported by the local-only modules (engine/frames.py, engine/cluster.py,
# engine/report.py, engine/cli.py).
numpy>=1.26,<3
scipy>=1.11,<2
fastapi>=0.110,<1
pydantic>=2.6,<3
